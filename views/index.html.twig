{% extends 'others/layout.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block contentheader %}
    <center><h1>Accueil</h1></center>
{% endblock %}

{% block contentmain %}
    <div class="row">
        <div class="col-md-3">
            <div class="box box-primary">
                <div class="box-header">
                    <h2 class="box-title">Recherche de traversées</h2>
                </div>
                <div class="box-body">
                    <div class="row">
                        {% if error > 400 and error < 404%}
                        <div class="col-md-12">
                            <div class="alert alert-warning">
                                <i class="fa fa-warning"></i>
                            <b>Alerte !</b>
                            {% if error == 401 %}
                                Veuillez sélectionner un lien
                            {% endif %}
                            {% if error == 402 %}
                                Veuillez entrer une date et une heure
                            {% endif %}
                            {% if error == 403 %}
                                Veuillez respecter le format de la date
                            {% endif %}
                            </div>
                        </div>
                        {% endif %}
                    </div>
                    <form method="post" action="search_crossing">
                        <div class="form-group">
                            <label>Date :</label>
                            <div class="row">
                                <div class="col-md-6">
                                    <div class="input-group">
                                        <div class="input-group-addon">
                                            <i class="fa fa-calendar"></i>
                                        </div>
                                        <input class="form-control" placeholder="dd/mm/yyyy" type="text" name="date"
                                               maxlength="10"/>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="form-group">
                            <label>Heure souhaitée :</label>
                            <div class="row">
                                <div class="col-md-4">
                                    <div class="input-group">
                                        <div class="input-group-addon">
                                            <i class="fa fa-clock-o"></i>
                                        </div>
                                        <input class="form-control" placeholder="hh" type="text" name="hour"
                                               maxlength="2"/>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="form-group">
                            <label>Lien</label>
                            <div class="row">
                                <div class="col-md-12">
                                    <select class="form-control" name="link">
                                        <option value="unselected" selected>---</option>
                                        {% for link in links %}
                                            <option value="{{ link.id }}">{{ link.startingHarbor.name }} &rarr; {{ link.arrivalHarbor.name }}</option>
                                        {% endfor %}
                                    </select>
                                </div>
                            </div>
                        </div>
                        <div class="form-group">
                            <input type="submit" class="btn btn-primary"/>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
{% endblock %}